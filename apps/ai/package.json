{
  "name": "@sdgi/ai",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn sdgi_ai.main:app --reload --port 5000 --app-dir src",
    "build": "echo 'python build is no-op'",
    "lint": "python3 -m ruff check src tests",
    "typecheck": "python3 -m mypy src",
    "test": "python3 -m pytest -q"
  }
}
